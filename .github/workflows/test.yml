name: Tests

on:
  push:
    branches:
     - master
  pull_request_target:
  workflow_dispatch:

jobs:   
  test:
    runs-on: ubuntu-latest
    environment: CI
    steps:
      - run: npm i
      - run: npm test
